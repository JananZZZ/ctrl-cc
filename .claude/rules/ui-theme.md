# UI Theme Rules
- Neo Calm Industrial: 4 themes (light, dark, pale-blue, warm-sand)
- Design tokens ONLY (`--cc-*`). No hard-coded colors.
- Errors show: what failed, which layer, why, what user can do, copy diagnostics.
- Every async action: immediate feedback + current phase + timeout + failure reason.
